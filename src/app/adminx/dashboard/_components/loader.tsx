import { Icon } from "@/lib/icons";

export const Loader = () => {
  return (
    <div className="flex items-center justify-center h-24">
      <Icon name="spinners-ring" className="text-teal-400" />
    </div>
  );
};
